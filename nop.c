#include "monty.h"

/**
 * nop - An useless function
 * @line_number: line read
 * @stack: double pointer to second array
 * Return: nothing
 */

void nop(stack_t **stack, unsigned int line_number)
{
	(void) (*stack);
	(void) line_number;
}
